# Optional — only needed for external API bases. Same-origin /api routes work without this.
# VITE_API_BASE_URL=http://localhost:5173

# Server-only — required for live Korean review extraction on Vercel.
OPENAI_API_KEY=
